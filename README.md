# Emacs Configuration

## To build Proof General

```shell
cd ~/.emacs.d/elisp/PG/
make clean
make
```

If on OSX, probably need:

```shell
make EMACS=/Applications/Emacs.app/Contents/MacOS/Emacs
```
